#include <stdio.h>
int main()
{
    int indice;
    int soma =0;
    int K;
    indice = 13;
    printf("Valor de K:");
    scanf("%d", &K);
    {
    while (K < indice)
    K = (K + 1); 
    soma = (soma+K);
    soma = soma;
    printf("%d", soma);
    return 0; 
    }
}
